export const setLastFuelPrice = (price) => ({
    type: 'SET_LAST_FUEL_PRICE',
    payload: price
  });
  
  export const setLastFullServicePrice = (price) => ({
    type: 'SET_LAST_FULL_SERVICE_PRICE',
    payload: price
  });